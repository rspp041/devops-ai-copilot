diff --git a/apps/sample-service/src/app.py b/apps/sample-service/src/app.py
index 1111111..2222222 100644
--- a/apps/sample-service/src/app.py
+++ b/apps/sample-service/src/app.py
@@ -1,9 +1,11 @@
 def add(a: int, b: int) -> int:
     """Simple function used for test generation demos."""
     return a + b

 def safe_divide(a: float, b: float) -> float:
     """Division with a clear error on divide-by-zero."""
-    if b == 0:
-        raise ValueError("b must not be 0")
+    if b == 0:
+        raise ValueError("b must not be 0")
     return a / b
